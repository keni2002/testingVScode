services:
  servidor:
    container_name: container_goodbye
    image: goodbye
    restart: always
    build:
      context: .
      dockerfile: elizabeth/Dockerfile
      target: baseOfLi
    ports:
      - "${PORT}:${PORT}"
